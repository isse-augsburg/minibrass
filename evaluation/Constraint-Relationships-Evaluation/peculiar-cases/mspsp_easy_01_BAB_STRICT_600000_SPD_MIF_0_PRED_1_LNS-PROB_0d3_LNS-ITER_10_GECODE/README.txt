We see that the search heuristic affects the correctnes (!?) of a model.

- When setting "mostImportantFirst = true;" in evaluation-conf.dzn, the problem is solved with penalty 3.
- If set to "false", the system incorrectly returns "UNSATISFIABLE"
