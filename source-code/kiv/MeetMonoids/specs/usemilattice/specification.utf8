enrich element with

;; sorts ;

constants ⊥ : element;

functions 
. + . : element × element → element 
;

predicates 
. ≤_U . : element × element 
;

;; procedures ;

;; variables ;

;; partial functions ;

;; partial predicates ;

;; induction ;

axioms
idemp : e + e = e;
comm: e + e0 = e0 + e;
assoc: (e + e0) + e1 = e + (e0 + e1);

neutral : e + ⊥ = e;

defOrder : e ≤_U e0 ↔ e + e0 = e0;
;; declaration

end enrich
